language: elixir

elixir:
  - 1.7
  - 1.6
  - 1.5
  - 1.4

env:
  global:
    - MIX_ENV=test

notifications:
  email: false

sudo: false

script:
  - mix test
  - mix credo
  - if [[ `elixir -v` = *"1.7"* ]]; then mix format --check-formatted; fi
